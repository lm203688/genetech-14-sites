# GeneTech 14站知识引擎 · 三维度审计报告（2026-08-31）

> 审计凭据：用户提供 PAT `ghp_rrBY…`（与 secrets.json 现有令牌相同，未真正轮换；scopes = `repo, workflow, delete_repo`）。
> 范围：完整性（结构/管线/构建）、价值性（数据护城河/商业化）、安全性（密钥泄露/公开仓风险）。

## 一、完整性 — 结构完整，但发现"总开关级"提交故障（已修复）

**结构 ✅**：`operations-plan / mcp-server / unified-license / api-guard / content / docs / shared / state` 齐全；30 站目录在；4 个 workflow 全部 active；MCP server `@genetech/data-mcp@1.1.0` 已打包；许可证页 `renderLicensePage` 存在；每站 `website/api/{index,entities}.json` 契约 intact。

**🔴 第七重根因（本次最关键发现）**：全部 **6 处** `git commit -m "...$(date -u +%Y-%m-%d-%H:%M:%S UTC)"` 中 `UTC` 被当成 `date` 的**多余操作数** → `date: extra operand 'UTC'` 报错，在 `bash -e` 下于 `git commit` 执行前中止；叠加 `continue-on-error: true` → job 显示绿色、产出**静默丢弃**。

- **影响面**：自该 bug 引入（约 08-06 起），data / domain / promo / intel / extra **全部自动化提交从未真正落库**。
- **铁证**：仓库 `reports/` 仅余 `report-data-backfill-2026-08-04/05/06.json`；08-31 00:05 那次算出 63,909 条新实体却未提交（报告文件在仓库中不存在，HEAD 仍停在 08-30 手动提交 `5b42338`）。
- 这意味着：此前修的六层门禁只让 backfill「算对」，但「提交」最后一环一直坏着，**数据始终没真正入库**——这才是冻结（49,879）至今未增长的真尾凶。

**修复（已推送，远端 `6f19574`）**：
1. 两 workflow 全部 6 处 date 格式改为 `date -u +'%Y-%m-%d-%H:%M:%S UTC'`（`UTC` 收进引号）。
2. 移除 data-accumulation 提交的 `continue-on-error: true`，使未来提交失败变红、不再静默。

> 远端 raw 复验通过：无未引号变体；data 步 `continue-on-error` 已消失。**下一次整点 backfill 将真正持久化数据**，护城河开始自动增长。

**其他**：
- 线上 catalog 仍 49,879 / 22 站（08-30 09:20 构建）——因 CF 构建预算门控把数据提交标 `[CI Skip]` 攒批，线上滞后仓库约 1 天（设计取舍，非故障）。
- `sites/` 为空遗留目录，commit 步骤已用 `-d` 守卫，不阻塞。

## 二、安全性 — 无历史泄露，但公开仓 + 过宽令牌需收敛

**✅ 好消息**：
- git 历史全 blob 精确扫描 `ghp_ / sk- / [REDACTED-CF-USER-API-TOKEN]… / github_pat_` **全部为空** → 真实密钥从未进历史。
- `.secrets/`、`.workbuddy/`、`tmp/ghops/`、`config/llm-bridge.local.json` 均 gitignore 且**未被跟踪**；`config/llm-bridge.json` 是空密钥模板；`unified-license/DEPLOY-SECRETS.md` 仅文档（无存活密钥）。

**⚠️ 待收敛**：
1. **仓库 PUBLIC + GitHub Secret Scanning 被禁用** → 任何未来误提交即全网可读，只能靠人工 vigilance。
2. **磁盘明文残留真实密钥**（gitignore 但应轮换）：`tmp/ghops/api_deploy_classic.mjs` 内真实 Cloudflare token `[REDACTED-CF-USER-API-TOKEN]`；`.secrets/pro_keys.json`、`pro_secret.txt`。
3. **所用 PAT 过宽**（含 `delete_repo`），建议改 fine-grained 最小集（`contents:write + actions:write`）。

## 三、价值性 — 护城河真实，此前被提交 bug 掐断增长

- **数据护城河真实**：30 垂直站 × 11 数据源（OpenAlex/arXiv/Crossref/S2/EuropePMC/PubMed/DOAJ/DataCite/Zenodo/CORE）× 6 层门禁（CLEAN 剔除废录+折叠重复 id → GUARD 策略 → QUALITY 字段契约）→ 单站目标 1 万、满配 30 万实体。
- **卖水人定位清晰**：结构化生物×AI 实体经 `@genetech/data-mcp` 供 AI 直接调用（Agent 原生），差异化于 Scite（引文网络）/ 秘塔·知网（中文文献）/ 晶泰（药物发现）。
- **商业化骨架就绪**：四层定价 + 虎皮椒支付 + 许可证页；支付闭环待用户注入虎皮椒凭据（Cloudflare Secret）。
- **弱点**：护城河此前因提交 bug 未自动增长（已修）；CORE 源未启用（少 1/11 源）；线上站点滞后仓库；DOI 可溯源度待提升。

## 四、风险清单与下一步

| 优先级 | 项 | 处置 |
|---|---|---|
| 🔴 已修 | 6 处 date 提交 bug + data 步 continue-on-error | 已推送 `6f19574`，待下次整点运行验证落库 |
| 🟡 | 轮换磁盘真实 CF token `[REDACTED-CF-USER-API-TOKEN]…` | 用户手动（Cloudflare 后台删旧建新） |
| 🟡 | PAT 降权为 fine-grained / 去 delete_repo | 用户手动 |
| 🟡 | 虎皮椒凭据注入 license.swarmlabs.tools | 用户手动（Cloudflare Secret） |
| 🟢 | CORE_API_KEY 启用第 11 源 | 用户申请并配 Secrets |
| 🟢 | 启用 GitHub Secret Scanning（公开仓） | 仓库 Settings → Security 开启 |

**结论**：项目工程完整性高、数据护城河真实且有差异化，安全性无历史泄露；唯一阻断性缺陷（自动化提交静默失败）已定位并修复，护城河即将恢复自动增长。剩余风险均为账号/资金类硬阻塞，需你本人操作。
