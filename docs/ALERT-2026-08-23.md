# 🚨 数据库扩张告警 — 2026-08-23

> 本文件是**邮件告警的替代品**。Agent Mail 授权已连续 2 天失效（08-22、08-23），告警邮件发不出去，
> 因此把告警内容单独落成文件，确保你在任何客户端都能看到。

## 一句话

🟡 **STALL ×6 + ⚠️ QUALITY** — 总实体 **49879**（+0 vs 08-22），22 站；
质量 URL 99% / 摘要 47% / DOI 0% / 去重 97%。**数据库已连续 7 天零增长。**

## 关键数字

| 指标 | 值 | 说明 |
|---|---|---|
| 总实体 | 49879 | 与 08-17 完全一致 |
| Δ 总实体（24h） | **+0** | 远低于 STALL 阈值 50 |
| 冻结天数 | **7 天**（08-17 起） | 连续第 6 次 STALL |
| 站点数 | 22 | 无变化 |
| 远端游标最后提交 | `75bfb85` @ 2026-08-14T12:48:37Z | **冻结满 9 天** |
| CI 状态 | 每小时 `success`，**零数据提交** | 绿色空转 |
| DOI 可溯源度 | **0%** | 持续低于 10% 红线 |
| 距 boost 期结束 | 2026-08-30（剩 7 天） | 目标 100k+ 实体几无进展 |

## 各站实体（全部持平，无一涨跌）

biocomputing 4767 · embodied-ai 2961 · alien-minerals 2774 · synbio-manufacturing 2711 ·
new-energy 2621 · quantum-computing 2553 · brain-science 2504 · agent-ecosystem 2423 ·
tcm-tools 2289 · semiconductor 2280 · nuclear-energy 2247 · deep-sea-tech 2227 ·
exo-science 2219 · ai4science 2216 · bionic-ai 2171 · life-science 1974 · low-altitude 1942 ·
genetech-tools 1581 · robot-parts 1458 · sat-6g 1393 · privacy-computing 1310 · spatial-computing 1258

## 三条链同时断（这是本次最需要你知道的事）

1. **扩量链断**：decide 步骤 cron 字符串漂移 → 数据涡轮分支永不命中，每天只跑约 2 轮而非 24~48 轮；
   且这 2 轮也零新增（疑检索词/数据源饱和）。
2. **CI 告警链断**：`notify-on-failure` 用 `if: failure()`，覆盖不到「跳过 / 软失败 / 跑通但零新增」→ CI 永远绿。
3. **巡检告警链断**：Agent Mail 授权失效 → 邮件发不出（08-22、08-23）。

→ 结果：**出问题时没有任何自动通道能叫醒你**，只能靠本文件和对话回复。

## 建议动作（按优先级）

1. 🔴 **重新授权 Agent Mail**：点 `workbuddy://agentmail` → 「更多 - 我的邮箱」重新授权。先修告警通道，否则后面所有问题都还是静默。
2. 🔴 **修 decide cron 漂移**：`ops.yml` 里把 `[[ "$SCHED" == "*/30 * * * *" ]]` 改成比对 `"0 * * * *"`（或改为「非 `30 12 * * *` 的 schedule 均视为数据轮」）。
3. 🟠 **手动 workflow_dispatch 跑一次 data**，看日志 `新增实体合计` 与游标 diff，判断是否真为检索词耗尽；若是则补检索词 / 加数据源。
4. 🟠 **加零新增守门**：连续 N 轮 `ADDED=0` 自动建 Issue，把「绿色空转」变成可见失败。
5. 🟡 **推送闭源 refactor 前先配 `ENGINE_TOKEN` Secret**，否则 5 条流水线会立刻全红（本地 HEAD `ca85ad1` 待爆雷）。
6. 🟡 **DOI 落盘**：backfill 写入时保留 Crossref / DataCite 的 DOI，解 DOI 0% 红线。

---

约束遵守：本次仅本地只读统计 + 本地日志写入 + 远端只读核查；**未改 CI 代码、未推送远端、未碰私钥**（游标归 CI 所有）。
