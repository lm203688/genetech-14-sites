# ⚠️ GeneTech 14站 · 数据库扩张告警 — 2026-08-29

**状态：🟡 STALL ×12 连续 + ⚠️ QUALITY（DOI 可溯源度 0%）**

本告警由「每日数据库扩张健康巡检」自动化生成。邮件告警通道自 2026-08-22 起连续 8 天授权失效、无法发送，此为补偿触达文件（与对话回复同步）。

## 核心指标（2026-08-29 06:00 GMT+8）
- 总实体：**49,879**（22 站），Δ = **+0** vs 2026-08-28（连续第 12 次 STALL，08-17 起 Δ=0 满 13 天）
- 质量：溯源URL完整度 **99%** / 摘要完整度 **47%** / DOI可溯源度 **0%**（<10% 红线）/ 跨源去重率 **97%**
- 摘要完整度较前次无下滑（47% 持平），但 DOI 可溯源度长期 0% 持续触红线。

## 远端实测证据（本日 GitHub API 只读核查，非推测）
- 远端 `state/backfill-cursor.json` 最后提交：`75bfb8589a35` · `[Bot] Intelligence update` · **2026-08-14T12:48:37Z** → 游标冻结约 **14 天**。
- 远端游标内容与本地**逐字节一致** → 非本地副本过期。
- master 末次提交：`6f28ccea93d1` · 2026-08-28T04:29:55Z（非 backfill）→ 仓库仍活动，**仅数据 backfill 管线停摆**。

## 根因（置信度）
1. 🔴 高 — ops.yml decide 步骤 cron 字符串漂移（`*/30 * * * *` 已于 08-07 改为 `0 * * * *` 但比对未改），数据涡轮分支永不命中，扩量频次塌缩至约 2 轮/天。
2. 🟡 中 — 即便这 2 轮也零新增且游标不动，疑「站点×检索词」分页空间耗尽，需补检索词/加源。

## 建议动作（本自动化按约束未改代码、未推远端）
1. 修 decide 漂移：把 `"*/30 * * * *"` 比对改为 `"0 * * * *"`。
2. 手动 `workflow_dispatch` 跑一次 data，看 `新增实体合计` 与游标 diff；若检索词耗尽则补检索词/加源。
3. 推送闭源 refactor（HEAD ca85ad1）前，先到 Settings → Secrets → Actions 配 `ENGINE_TOKEN`（缺则 5 条流水线全红）。
4. 补真告警：加「连续 N 轮 ADDED=0 自动建 Issue」守门，覆盖 `notify-on-failure` 覆盖不到的静默成功。
5. DOI 可溯源度长期 0%：backfill 落盘时保留 Crossref / DataCite 的 DOI。
6. **⚠️ 优先恢复告警触达**：前往 workbuddy://agentmail 重新授权 Agent Mail 邮箱（已连续 8 天失效），否则用户将持续收不到邮件告警。

> 约束遵守：未改 CI 代码、未推送远端、未碰私钥。
