# ⚠️ GeneTech 14站 · 数据库扩张告警 — 2026-08-26

> 本告警由「每日数据库扩张健康巡检」自动化生成。邮件通道（Agent Mail）自 2026-08-22 起连续第 5 天授权失效、发送被拒，此为**补偿触达通道**（独立告警文件 + 对话回复），请确保用户实际看到。

## 状态：🟡 STALL ×9 连续 + ⚠️ QUALITY

- **总实体**：49879（连续第 10 天冻结，08-17 起 Δ=0）
- **站点**：22（无增减）
- **本日 Δ**：总实体 +0 / 站点 +0 / 各站逐项持平（两路 Node 统计一致：db-quality-check.mjs 与直数均为 49879 / 22 站）
- **质量指标**：URL 完整度 99% / 摘要完整度 47%（无下滑）/ DOI 可溯源度 0%（<10% 红线，触 ⚠️ QUALITY）/ 去重率 97%
- **来源分布**：arxiv 8684 / openalex 8573 / crossref 12093 / pubmed 6106 / europepmc 12679 / semanticscholar 671 / datacite 442 / preprints 631

## 根因（已定位，至今未修复）

1. 🔴 高置信 — `ops.yml` decide 步骤仍比对旧 cron 字符串 `*/30 * * * *`（2026-08-07 已改为 `0 * * * *`），数据涡轮分支永不命中，扩量频次由预期 24~48 轮/天塌缩至约 2 轮/天。
2. 🟡 中置信 — 即便这 2 轮也新增 0 条且游标未推进，疑「站点×检索词」分页空间已走完，需扩检索词或加源。
3. ⚠️ 待爆雷 — 本地 HEAD `ca85ad1` 闭源 refactor 未推送；ops.yml 新增 `Fetch private core engine` 步骤缺 `ENGINE_TOKEN` 即 `exit 1`，推送前须先配 Secrets。

## 告警通道状态

- 本地日志（docs/db-health-log.md）：✅ 已追加 08-26 行 + 顶部告警段落
- Agent Mail 邮件：❌ **连续第 5 天失效**（08-22~26，GetMe 被拒「当前请勿重试」）
- 对话回复：✅ 本回合 ALERT 块
- 补偿文件：✅ docs/ALERT-2026-08-26.md（本文件）

## 建议动作（本自动化按约束未改代码、未推远端）

1. 修 decide 漂移：把 `"*/30 * * * *"` 比对改为 `"0 * * * *"`。
2. 手动 `workflow_dispatch` 跑一次 data，看 `新增实体合计` 与游标 diff；若检索词耗尽则补检索词/加源。
3. 推送闭源 refactor 前，先到 Settings → Secrets → Actions 配好 `ENGINE_TOKEN`。
4. 补真告警：加零新增守门（连续 N 轮 `ADDED=0` 自动建 Issue），覆盖「跳过/软失败/零新增」静默场景。
5. DOI 可溯源度长期 0%：backfill 落盘时保留 Crossref / DataCite 的 DOI。
6. **立即重新授权 Agent Mail（workbuddy://agentmail）**，恢复告警邮件通道，否则后续 STALL 用户将再次收不到任何告警。
