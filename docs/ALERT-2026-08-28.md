# ⚠️ 数据库扩张告警 — 2026-08-28

**状态：🟡 STALL ×11 连续 + ⚠️ QUALITY**

| 指标 | 本次 | 上次(08-27) | 变化 |
|---|---|---|---|
| 总实体 | 49879 | 49879 | +0（连续第 12 天冻结） |
| 站点数 | 22 | 22 | 0 |
| 溯源URL完整度 | 99% | 99% | 持平 |
| 摘要完整度 | 47% | 47% | 持平（未下滑 >5pt） |
| **DOI可溯源度** | **0%** | **0%** | **触质量红线（<10%）** |
| 跨源去重率 | 97% | 97% | 持平 |

## 根因（未修复，08-19 已定位）
1. 🔴 高 — ops.yml `decide` 步骤仍比对旧 cron 字符串 `*/30 * * * *`（2026-08-07 已改为 `0 * * * *`），数据涡轮分支永不命中，扩量频次由 24~48 轮/天塌缩至约 2 轮/天。
2. 🟡 中 — 即便这 2 轮跑完也新增 0 条且游标未推进；疑「站点×检索词」分页空间耗尽，需补检索词/加源。
3. ⚠️ 静默失败 — ops 每小时 run 均报 success，`notify-on-failure` 的 `if: failure()` 永不触发，正是 08-18 STALL 未被发现的机制原因。

## 远端复核（CI 侧冻结确证）
- 远端 `state/backfill-cursor.json` 最后提交 `75bfb85 / 2026-08-14T12:48Z`，与本地逐字节一致（08-25 已确认）→ **游标冻结满 14 天**。
- ops.yml 仍每小时 completed/success 但零数据提交 → 绿色空转。

## ⚠️ 告警邮件通道已中断 7 天（08-22~08-28）
Agent Mail 授权失效（GetMe 返回「请前往 workbuddy://agentmail 重新授权，当前请勿重试」），本日邮件告警**再次发不出**。
后果：用户在本通道恢复前收不到任何 STALL/QUALITY 邮件 —— 重演 08-18 未被发现的同类失效。
**请用户尽快重新授权 Agent Mail 邮箱（workbuddy://agentmail），否则本自动化的「告警触达」承诺名存实亡。**

## 建议动作（本自动化按约束未改代码、未推远端）
1. 修 `decide` 漂移：把 `"*/30 * * * *"` 比对改为 `"0 * * * *"`。
2. 手动 `workflow_dispatch` 跑一次 data，看 `新增实体合计` 与游标 diff；若检索词耗尽则补检索词/加源。
3. 推送闭源 refactor（HEAD `ca85ad1`）前，先到 Settings → Secrets → Actions 配 `ENGINE_TOKEN`，否则 5 条流水线将全红。
4. 补真告警：加零新增守门（连续 N 轮 `ADDED=0` 自动建 Issue）。
5. DOI 可溯源度长期 0%：backfill 落盘时保留 Crossref/DataCite 的 DOI。

---
由「每日数据库扩张健康巡检」自动化于 2026-08-28 生成。本文件为邮件通道失效时的补偿触达通道。
