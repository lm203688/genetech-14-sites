# 🟡⚠️ ALERT — GeneTech 14站数据库扩张巡检（2026-08-27）

> 本文件为「每日数据库扩张健康巡检」在 **Agent Mail 邮件通道连续第 6 天失效** 时的**补偿触达通道**。请用户务必看到。

## 状态判定
- **总实体**：49879（与 08-26 持平，Δ = 0）
- **站点数**：22（与 08-26 持平，Δ = 0）
- **连续停滞**：🟡 STALL **连续第 10 次**（自 08-17 起冻结满 **11 天**，每日 Δ=0）
- **质量红线**：⚠️ QUALITY — **DOI 可溯源度 0%**（< 10% 红线，持续触红）
- **质量概览**：URL 99% / 摘要 47% / DOI 0% / 去重 97%
- **各站涨跌**：22 站逐项与 08-26 完全一致，无任何站点增长或回退
- **来源分布**：arxiv 8684 / openalex 8573 / crossref 12093 / pubmed 6106 / europepmc 12679 / semanticscholar 671 / datacite 442 / preprints 631（与 08-26 逐项一致）

## 告警触发条件（满足即须触达，绝不静默）
- 🔴 REGRESSION：否（总实体未下降）
- 🟡 STALL 连续 ≥ 2 次：**是（×10）**
- ⚠️ QUALITY 红线：**是（DOI 0%）**

→ **三重条件命中，必须告警。**

## 告警邮件通道状态（关键）
- ❌ **Agent Mail 授权连续第 6 天失效**（08-22 ~ 08-27）。
- 本日调用 `GetMe` 仍返回：「授权已失效，请前往 workbuddy://agentmail 重新授权，当前请勿重试」。
- **后果**：用户已 6 天未收到任何 STALL/QUALITY 告警邮件，重演 2026-08-18 未被发现的同类失效。
- **需用户操作**：点击 **workbuddy://agentmail** 重新授权 Agent Mail 邮箱，恢复告警邮件触达。

## CI 侧根因（08-19 已定位，至今未修复）
1. 🔴 高 — `ops.yml` decide 步骤仍在比对旧 cron 字符串 `"*/30 * * * *"`（已于 2026-08-07 改为 `"0 * * * *"`），数据涡轮分支永不命中，`should_run_data` 仅靠 `HOUR==12` 兜底 → 扩量频次由预期 24~48 轮/天塌缩至约 2 轮/天。
2. 🟡 中 — 即便这 2 轮跑完也新增 0 条且游标未推进，疑「站点×检索词」分页空间已耗尽（检索词/数据源饱和），需补检索词或加源。
3. ⚠️ 待爆雷 — 本地 HEAD `ca85ad1` 闭源 refactor 未推送；`ops.yml` 新增 `Fetch private core engine` 步骤缺 `ENGINE_TOKEN` 即 `exit 1`，**一旦推送该提交而 Secrets 未配，5 条流水线将全红**。

## 远端复核（CI 侧冻结，非本地过期）
- 依据 08-25 已确认证据：远端 `state/backfill-cursor.json` 与本地游标**逐字节一致**（14 键同值），最后提交 `75bfb85 / 2026-08-14T12:48Z` → 游标冻结满 **13 天**。
- `ops.yml` 仍每小时运行、逐轮 `completed / success` 但零数据提交 → 「绿色空转」，静默失败机制未变。

## 建议动作（本自动化按约束未改代码、未推远端、未碰私钥）
1. 修 decide 漂移：把 `"*/30 * * * *"` 比对改为 `"0 * * * *"`。
2. 手动 `workflow_dispatch` 跑一次 data，看日志 `新增实体合计`；若为检索词耗尽，则补检索词 / 加数据源。
3. 推送闭源 refactor 前，先配 `ENGINE_TOKEN` Secret。
4. 加零新增守门：连续 N 轮 `ADDED=0` 自动建 Issue（补 `notify-on-failure` 覆盖不到的静默失败）。
5. 落 DOI 字段：backfill 入库时保留 Crossref / DataCite 的 DOI，消除质量红线。
6. **重新授权 Agent Mail（workbuddy://agentmail）**，恢复告警邮件触达。

---
*本 ALERT 由「每日数据库扩张健康巡检」自动化生成（2026-08-27 06:00 GMT+8）。主日志见 docs/db-health-log.md，快照见 state/db-health-history.json。本自动化严格只读本地 + 写本地日志 + 补偿触达，绝不自行推送远端。*
