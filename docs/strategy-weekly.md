# GeneTech 14 站 · 战略指标周摘要（自动化维护）

> 生成：每周日 10:00 自动化（automation-1786923647158）
> 方法：读各站 `entities.json` + `node tools/db-quality-check.mjs --json` + `content/blog/` 对比 + 许可证页四层定价 + 竞品 docs（competitor-models.md / profitable-model-alignment.md）
> 三视角：用户 / 投资人 / 行业专家
> 约束：不注册账号、不碰私钥、不替用户签约；只做内容生成与本地文件维护

---

## 2026-08-23（第 1 期 / 基线期）

> ⚠️ **§4.5 运维/质量改进仍未部署（仅编码未推送），须执行安全推送让 CI 生效** —— 当前 DOI 可溯源度仍为 0%，质量改进代码已写但未推送，CI 未重建，故未生效。

### 一、本周关键指标

| 指标 | 数值 | 说明 |
|---|---|---|
| 数据库总实体数 | **49,879** | 较历史基线 47,566 ↑ 2,313 |
| 站点数 | **22** | 覆盖 AI4S / 具身 / 量子 / 半导体 / 低空 / 6G / 生物计算 / 合成生物等 |
| URL 完整度 | 99% | 稳，基本全有可访问来源 |
| 摘要完整度 | **47%** | 偏低，约半数实体缺摘要，损伤免费层可读性 |
| DOI 可溯源度 | **0%** 🔴 | 硬伤，直接指向 §4.5 未部署（本周 P0） |
| 去重率 | 97% | 稳，重复控制良好 |
| 唯一键 | 48,249 | — |
| 数据来源分布 | crossref 12.1k / europepmc 12.7k / arxiv 8.7k / openalex 8.6k / pubmed 6.1k / preprints 0.63k / semanticscholar 0.67k / datacite 0.44k | 8 源混合检索 |

### 二、GEO 博客产出

- 总数（`content/blog/`）：**4 篇**（1 篇 roundup + 3 篇前沿权威长文）
- 本周新增（2026-08-17 起）：**1 篇** —— `biocomputing-2025-2026.md`（2026-08-17，带 X 算法排名信号结构：钩子问题 / 配图 / 收藏级长文 / 垂直关键词 / 转化 CTA / 避罚）

### 三、许可证页四层定价可见性

- ✅ **可见**：免费 / Pro 买断（¥9.9·¥39.9·¥199）/ 社群（¥299）/ 企业授权（¥20万–500万/年）
- 转化路径清晰，CTA 闭环已建；社群载体 + `api-guard` 企业路由仍需用户侧开通才能兑现顶端 LTV。

### 四、竞品动态（锚定已核实事件/人物，不编造）

- **晶泰科技（2228.HK）** 2025 年报（2026-03-25）：营收 ¥8.03 亿（+201.2%），年内利润 ¥1.35 亿、经调整 ¥2.58 亿，AI4S 首家盈利港股；DoveTree $6B、礼来 $3.45 亿合作 → 卖水人 + 里程碑 + 分成被真实验证。
- **Schrödinger（SDGR）** 2025 财报（2026-02-25，CEO Ramy Farid）：营收 $255.9M（+23.3%），软件 $199.5M 占 78%、毛利 74%，但公司净亏 $103.3M → 授权高毛利、仍需熬投入期。
- **小鹅通** 2025 招股书（创始人鲍春健）：经调整净利 ¥1.07 亿、NRR 117.5%，但 IFRS 净亏 ¥6395 万 → 私域高留存。
- **秘塔 AI（闵可锐）**：2025 创始人口径盈利（待审计），自训模型 + 智算中心降本约 40% → 国内愿为结构化数据低价付费。
- **知识星球（洪灏样本）**：¥899/年 × 1.1 万 ≈ ¥989 万流水（6 周）→ 年费社群抬 LTV。
- **Cursor** 2025 负毛利 ≈ -30%（租 Anthropic ~$6.5 亿、收入 ~$5 亿）→ 纯租模型毛利陷阱，本项目天然规避（数据第一方、无推理成本）。

### 五、三视角快评

**用户视角**：数据护城河在加深——实体数 49,879（较基线 +2,313）、22 站全覆盖、去重率 97%，免费层 GEO（llms.txt/JSON-LD + 4 篇权威长文）持续扩大被 AI 引擎引用的概率。免费层够用（URL 99%、数据全开放），但摘要完整度仅 47% 拉低免费体验。买断/社群/企业四层定价在许可证页均可见、CTA 闭环已建，转化路径清晰；唯一卡点在用户侧未开通社群载体与 `api-guard` 企业路由。

**投资人视角**：卖水人逻辑成立——晶泰靠「平台授权 + 里程碑 + 分成」首盈（复购 >75%、覆盖 17/20 大药企）、Schrödinger 软件毛利 74%，本项目企业数据授权（$20万–500万/年）与之同构。LTV 抬升路径已备（¥299 社群卡 + 企业授权区块），但社群载体未开通、尚未兑现金字塔顶端。最关键：我们不租第三方模型，无推理/模型成本，边际成本 ≈0，结构性规避 Cursor -30% 负毛利陷阱，也比秘塔（仍养自训模型/智算中心）更轻。

**行业专家视角**：前沿雷达指向正确——GEO 长文已覆盖 AI4S 范式转移、基因编辑 2.0、生物计算；`docs/frontier-discoveries.md` 明确把 **AlphaFold 3 / MICrONS / High-NA EUV / 固态电池 / PERT** 列为 2025-2026 重点回填方向。数据飞轮可持续（CI 每小时扩量、49,879 实体）。但**质量红线未守住**：DOI 可溯源度 0% 是硬伤，摘要完整度 47% 也偏低——飞轮「量」在涨、「质」需补，否则可溯源性与引用可信度受损。

### 六、建议提升（本周行动）

- 🔴 **P0｜部署 §4.5 运维/质量改进（DOI 可溯源）**：DOI 可溯源度仍为 0%，说明 §4.5 质量改进仅编码未推送、CI 未生效。须执行安全推送（在用户环境用有效 PAT 跑 `node tools/api-push.mjs`，或 `git push`），让 CI 重新构建并写入 DOI 溯源。
- **P1｜补摘要完整度（47% → 目标 70%+）**：约半数实体缺摘要，损伤免费层可读性与 GEO 引用质量；纳入扩量引擎的摘要回填 job。
- **P1｜开通社群载体 + `api-guard` 企业路由**：解锁 ¥299 社群 LTV 与企业授权计量（逻辑 D / B）。
- **P2｜认领 Glama / Smithery MCP 目录**：低成本分发，放大免费层获客。
- **持续｜守护数据库飞轮**：CI 每小时扩量、游标持久，朝 100k+ 目标；勿手动删数据。

### 七、部署 / 推送状态

- GitHub（`api.github.com`）**网络可达**（HTTP 200）。
- 但本环境 `git credential fill` 取不到凭据 → **无法自动推送**。
- ➡️ **待用户手动推送**：在已配置有效 PAT 的环境运行 `node tools/api-push.mjs`（或 `git push`），触发 Pages 重建。本摘要已写入本地 `docs/strategy-weekly.md` 并更新 `docs/geo-promotion-tracker.md`。

---

## 2026-08-23（第 2 期 / 首个定时执行）

> ⚠️ **§4.5 运维/质量改进仍未部署（仅编码未推送），须执行安全推送让 CI 生效** —— 线上 `entities.json` 一手抽样确认实体字段仅 `id/name/source/abstract/url/authors/tags/confidence/sites/publishedDate/addedAt`，**无 `doi` 字段**，DOI 可溯源度实测 0%。
>
> 🔴 **本期新增更硬的红标：数据飞轮已停转 17 天。** 线上 `api/catalog.json`（generatedAt 2026-08-22T04:00）总实体 49,879 与本地完全一致；22 站中 21 站 `lastUpdated` 停在 **2026-08-06**，仅 biocomputing 为 2026-08-14。护城河已停止加深。

> 说明：W1 于本日 09:55 建立基线，本期为首个定时执行（同日 10:00），故量化指标 Δ=0 属预期，本期价值在于**线上实况核验 + 停转根因定位**。

### 一、本周关键指标（对比 W1）

| 指标 | 本期 | W1 | Δ |
|---|---|---|---|
| 数据库总实体数 | 49,879 | 49,879 | **0** |
| 线上 catalog 实体数 | 49,879（2026-08-22T04:00） | — | 线上=本地，无待部署增量 |
| 站点数 | 22 | 22 | 0 |
| URL 完整度 | 99% | 99% | 0 |
| 摘要完整度 | 47% | 47% | 0 |
| DOI 可溯源度 | **0%** 🔴（线上一手确认无 doi 字段） | 0% | 0 |
| 去重率 / 唯一键 | 97% / 48,249 | 97% / 48,249 | 0 |
| 各站数据鲜度 | 21 站停留 **2026-08-06**（17 天）、biocomputing 2026-08-14（9 天） | 未核 | 🔴 新发现 |

### 二、GEO 博客产出

- 总数（`content/blog/`）：**4 篇**（未变）
- **本周新增：0 篇**。最近一篇仍是 `biocomputing-2025-2026.md`（2026-08-17，已计入 W1）。
- 远端每日仅有 `[Bot] Intelligence update`（2 文件）提交，**未产出新长文** → GEO 内容飞轮与数据飞轮同步降速。

### 三、许可证页四层定价可见性

- ✅ 四层均可见（`tools/build-site.mjs` 实测命中）：免费 / Pro 买断 `¥9.9`×3 `¥39.9`×2 `¥199`×3 / 社群 `¥299`×2 / 企业授权 `20万`×2 `500万`×1。
- 定价与 CTA 无回退；瓶颈仍在用户侧社群载体与 `api-guard` 企业路由未开通。

### 四、竞品动态

本周无新增可核实公开事件，锚点沿用 W1（晶泰 2025 首盈 ¥2.58 亿经调整 / DoveTree $6B；Schrödinger 软件毛利 74% 仍净亏 $103.3M；小鹅通 NRR 117.5%、经调整净利 ¥1.07 亿；秘塔 AI 闵可锐口径盈利；知识星球洪灏 ¥899×1.1 万；Cursor 2025 负毛利 ≈-30%）。结论未变：卖水人逻辑成立，纯租模型是毛利陷阱。

### 五、部署链路实况核验（本期重点）

| 检查项 | 实测结果 |
|---|---|
| `api.github.com` | HTTP 200 可达 |
| 远端 master | `178696e`（2026-08-22T12:23，`[Bot] Intelligence update`） |
| 本地 `origin/master` 引用 | `d4627c8` — **严重陈旧**，「落后 49 提交」是假象 |
| W1 判断修正 | 远端**已含** `security: 脱敏`(68dfbe1)、`fix(backfill): 游标停滞根因`(20cd774)、`docs+feat: v1.2`(de32879)，均已于 2026-08-18 推送（SHA 不同因走 api-push 造 commit） |
| 真正未推送 | 本地 `ca85ad1`（核心引擎闭源）+ `9451791`（W1 摘要）+ 8 个文件未提交改动 |
| GitHub Actions | 近 25 次运行**全部 success**（每小时一轮）——**假绿**：跑的是 intelligence，不是扩量 |
| 停滞看门狗 | 未告警。因看门狗嵌在 data job 内部，**data job 根本没被选中执行 → 看门狗也不跑**，构成监控盲区 |

### 六、飞轮停转根因（已定位，修复在本地未提交）

`.github/workflows/ops.yml` 的 "Decide which pipelines to run" 步骤硬编码比对 `SCHED == "*/30 * * * *"`，而 cron 已于 2026-08-07 改为 `0 * * * *` → **该分支永不命中**，每小时轮不跑数据扩量，仅剩每日 12:30 UTC 一轮兜底。本地已修为「非全量轮的 schedule 一律视为数据轮」（对后续调频免疫），但**未提交、未推送**。

⚠️ **推送顺序陷阱（必须先拍板）**：本地 `ca85ad1` 会从公开仓删除 `operations-plan/` 等引擎目录（远端目前仍存在，HTTP 200）。若在 **ENGINE_TOKEN 未配置**时推送 `ca85ad1`，且未同时提交本地 `.gitignore` 回退（把引擎重新入库作公开兜底），CI 将报「无可用引擎」→ **全线瘫痪**。两条路需用户拍板：
- **方案 A（保闭源，推荐）**：先在仓库 Secrets 配 `ENGINE_TOKEN`（fine-grained PAT，仅 `genetech-14-engine` 的 contents:read）→ 再提交 ops.yml 修复 → 推送 → `workflow_dispatch` 验证一轮有新增实体。
- **方案 B（放弃闭源）**：提交本地 `.gitignore` 回退，引擎重新入公开仓作兜底，零 Secrets 依赖，但核心引擎公开。

（本期不代拍板：涉及开源/闭源资产决策。）

### 七、三视角快评

**用户视角**：护城河本周**未加深**——实体数与线上完全一致（49,879，Δ0），21 站数据鲜度停在 17 天前，`docs/domain-graph.html` 领域图谱可视化的仍是 08-06 的存量。免费层「够用但不新鲜」：URL 完整度 99%、数据全开放、GEO 长文 4 篇仍可被引擎引用，但对"2026 下半年有什么新突破"这类查询已开始失真。买断/社群/企业四层定价在许可证页均可见、CTA 闭环完整，转化路径清晰度未退化；卡点仍是社群载体与 `api-guard` 企业路由未开通。

**投资人视角**：卖水人逻辑本身没变坏——晶泰 2025 首盈（营收 ¥8.03 亿 +201.2%，经调整净利 ¥2.58 亿）与 Schrödinger 软件毛利 74% 双向验证「平台/数据授权」可获利，我们的企业授权（¥20 万–500 万/年）与之同构；且不租第三方模型、边际成本 ≈0，结构性避开 Cursor 2025 负毛利 ≈-30% 的陷阱，也比仍养自训模型与智算中心的秘塔更轻。但**本期尽调口径要扣分**：资产（实体数）零增长 17 天、CI 假绿、DOI 0%，意味着"数据飞轮自动增值"这条估值故事目前是**不可验证**的。LTV 抬升路径（¥299 社群 + 企业授权）仍停在纸面，未兑现金字塔顶端。

**行业专家视角**：前沿覆盖经**线上实体一手抽样**，5 个方向全部真实落库：AlphaFold（ai4science 40 / life-science 6）、Perturb-seq/PERT（各站 12–17）、连接组含 MICrONS（brain-science connectome 324 / MICrONS 2）、High-NA EUV（semiconductor 8）、固态电池（new-energy 240）。方向判断正确，**最薄环节是 High-NA EUV（仅 8 条）**。但数据飞轮**已不可持续**：17 天零新增意味着 2026 年 8 月后的新突破无法进库，前沿雷达会随时间钝化。质量红线**未守住且未改善**：DOI 0%、摘要 47% 两项与 W1 持平，"量停 + 质不动"是本期最坏组合。

### 八、建议提升（本周行动）

- 🔴 **P0-1｜恢复数据飞轮（拍板 A/B 后推送 ops.yml decide 分支修复）**：这是本期第一优先，优先级高于 DOI。修复已在本地写好，只差「拍板 + 提交 + 推送 + workflow_dispatch 验证一轮有新增」。
- 🔴 **P0-2｜部署 §4.5 DOI 可溯源**：线上确认 entities 无 `doi` 字段，DOI 0% 属真实缺失（非本地陈旧）。crossref/pubmed/europepmc/datacite 四源原生带 DOI（合计 31,320 条，占 63%），回填成本低、收益直接（可溯源性 = 引用可信度 = 企业授权说服力）。
- 🔴 **P0-3｜补监控盲区**：新增**独立于 data job** 的巡检——「总实体数 7 天无变化」或「任一站 lastUpdated 超 10 天」即开 Issue。现有零新增看门狗嵌在 data job 内，job 不跑则永不告警，本次 17 天停摆就是这样被漏掉的。
- **P1｜摘要完整度 47% → 70%+**：约半数实体缺摘要，直接损伤免费层可读性与 GEO 引用质量。
- **P1｜恢复每周 GEO 长文**：本周产出 0 篇，内容飞轮同步降速；建议补 High-NA EUV / 固态电池方向长文（既补最薄前沿环节，又产 GEO 资产）。
- **P2｜开通社群载体 + `api-guard` 企业路由**；**P2｜认领 Glama / Smithery MCP 目录**。

### 九、部署 / 推送状态

- `api.github.com` **可达**（HTTP 200）。用户已于 2026-08-23 晚提供有效 PAT。
- **本次已推送（仅文档，外科手术式）**：`docs/strategy-weekly.md` + `docs/geo-promotion-tracker.md` 两文件经 REST API 直接上 `master`，**不触碰任何 workflow / 引擎目录** → Pages 重建触发，CI 瘫痪风险为 0。
- **未推送（待用户拍板 A/B 后再推）**：其余 6 个本地已提交改动（`.github/workflows/ops.yml`、`pages-deploy.yml`、`.gitignore`、`tools/deploy-independent-workers.mjs`、`docs/audit-closed-loops.md`、`state/db-health-history.json`）。其中 `ops.yml`/`pages-deploy.yml` 为 `ca85ad1`（核心引擎闭源）引入，很可能依赖 `ENGINE_TOKEN`；未配该 Secret 前推送有 CI 报「无可用引擎」风险。推荐序列：**方案 A（保闭源，推荐）** 配 `ENGINE_TOKEN` → 提交 ops.yml 修复 → 推送；或 **方案 B（放弃闭源）** 回退 `.gitignore` 让引擎重新入公开仓。
- 说明：`api-push.mjs` 本身删除安全（只打补丁、不删远端文件），但为避免引入未配置的 `ENGINE_TOKEN` 依赖，本次仅推 2 文档。

---

## 2026-08-30（第 3 期 / 本周定时执行）

> ⚠️ **DOI 可溯源度仍为 0% + 数据库连续冻结 13+ 天**：线上 catalog 总实体 49,879 与 2026-08-17 基线完全重合，21 站 `lastUpdated` 停在 2026-08-06、biocomputing 停在 2026-08-14。冻结真尾凶（第六重）`sites/` 目录在 CI 全新 checkout 不存在 → `git add sites/` pathspec 致命失败(rc=128) → 0 暂存 → 入库冻结，已本地修复（commit `410bff4`+`0104751`+`76971e2` 策略 v2+`operations-plan/pipeline-data-backfill.js` 容量 cap+`tools/guard-eval.mjs`），**仍仅编码未部署**，须安全推送让 CI 生效（本周 P0-1）。纠正 W1/W2 红标：§4.5 质量门禁字段契约修复（`6def99b`）**已部署**，DOI 0% 的真实阻断是「冻结无新数据入库」，非质量代码未部署。

### 一、本周关键指标（对比 W2）

| 指标 | 本期 | W2 | Δ |
|---|---|---|---|
| 数据库总实体数 | 49,879 | 49,879 | **0**（冻结第 13+ 天） |
| 线上 catalog 实体数 | 49,879（generatedAt 2026-08-29T10:12Z） | 49,879 | 0 |
| 站点数 | 22 | 22 | 0 |
| URL 完整度 | 99% | 99% | 0 |
| 摘要完整度 | 47% | 47% | 0 |
| DOI 可溯源度 | **0%** 🔴 | 0% | 0（冻结导致无新数据带 DOI 入库） |
| 去重率 / 唯一键 | 97% / 48,249 | 97% / 48,249 | 0 |
| 各站数据鲜度 | 21 站 2026-08-06、biocomputing 2026-08-14 | 同 | 🔴 冻结延续 |
| 来源分布 | crossref 12.1k / europepmc 12.7k / arxiv 8.7k / openalex 8.6k / pubmed 6.1k / semanticscholar 0.67k / datacite 0.44k / preprints 0.63k | 同 | 0 |

### 二、GEO 博客产出

- 总数（`content/blog/`）：**7 篇**（1 roundup + 4 前沿权威长文 + High-NA EUV + life-science + synbio-manufacturing）
- **本周新增：2 篇**（均 2026-08-24，周一 GEO 自动化产出）：
  - `life-science-2025-2026.md` — 生命科学 2025–2026（空间蛋白组 / 单细胞基础模型 / 合成生物 / 类器官）
  - `synbio-manufacturing-2025-2026.md` — 合成生物制造 2025–2026（Biofoundry / DBTL 流水线）
- 2 篇均默认带 X 算法排名信号结构（钩子问题 / 收藏级长文 / 垂直关键词 / 转化 CTA / 避罚）。

### 三、许可证页四层定价可见性

- ✅ 四层均可见（与 W1/W2 一致）：免费 / Pro 买断 `¥9.9`·`¥39.9`·`¥199` / 社群 `¥299` / 企业授权 `¥20万–500万/年`。
- 定价与 CTA 无回退；瓶颈仍在用户侧社群载体与 `api-guard` 企业路由未开通。

### 四、竞品动态（锚定已核实事件/人物，不编造）

本周无新增可核实公开事件，锚点沿用 W1/W2：晶泰 2025 首盈（营收 ¥8.03 亿 +201.2%，经调整净利 ¥2.58 亿；DoveTree $6B、礼来 $3.45 亿合作）/ Schrödinger 软件毛利 74% 仍净亏 $103.3M（CEO Ramy Farid）/ 小鹅通 NRR 117.5%、经调整净利 ¥1.07 亿（鲍春健）/ 秘塔 AI（闵可锐）口径盈利、自训模型降本约 40% / 知识星球（洪灏样本）¥899×1.1 万 ≈ ¥989 万流水 / Cursor 2025 负毛利 ≈-30%（租 Anthropic ~$6.5 亿）。结论未变：卖水人逻辑成立，纯租模型是毛利陷阱。

### 五、运维自愈回顾（消费 `.workbuddy/memory/automation-digest/2026-08-30.md`）

- **每日闭环 #1（2026-08-30 08:00）状态**：🔴 入库冻结第 13 天，但根因已定位并部分修复部署。
- **自愈动作（已执行）**：① 修复 `ops.yml:284` `git add` 目录级 glob → 文件级 `*/website/api/entities.json` `*/website/api/index.json`；② 定向单文件 Git Database 提交（base=远端 6def99b，仅覆盖 ops.yml）→ 远端 HEAD=`4ca84db`；③ 触发自愈 `push.mjs dispatch ops.yml pipeline=data-accumulation` → run `33283138461`（约 40 min）。
- **验证结果（本期复核）**：远端 catalog 仍 49,879、lastUpdated ≤ 2026-08-14 → `4ca84db` 仅修 website/api glob**未解真尾凶**；真尾凶是 `sites/` 目录 pathspec（CI 全新 checkout 无 `sites/` → rc=128 → 0 暂存），本地 `410bff4` 已用 `if [ -d sites ]; then git add sites/; fi` 加固，**仍未部署**。
- **GUARD/QUALITY 体检（dispatch 33236340549 + 33276085461）**：✅ 全部 22 站策略放行 + 质量通过（685a89a/6def99b 生效），GUARD_BLOCKED/QUALITY_BLOCKED 已排除 → 三层门禁仅剩 git 提交层失效。
- **升级建议（承接为本周 P0/P1）**：① 其它 pipeline `git add` 文件级 glob（ops.yml:369/501/562 未含 website/api 路径）；② 门禁可观测性——三层均显式打印「实际入库条数」，避免 `No changes to commit` 掩盖故障；③ 提升期决策（见 §七/1d）；④ 质量红线阈值改为「较基线降 >5pt」避免 DOI 0% 基线误报（每日闭环已建议）。

### 六、三视角快评

**用户视角**：数据护城河本周**仍停更**——实体数 49,879 与 2026-08-17 基线完全重合、连续冻结 13+ 天，`docs/domain-graph.html` 领域图谱可视化的仍是 08-06 存量；对"2026 下半年有什么新突破"类查询继续失真。免费层仍"够用但不新鲜"：URL 99%、7 篇 GEO 长文（本周 +2）持续扩大被 AI 引擎引用概率。买断/社群/企业四层定价均可见、CTA 闭环完整；卡点仍是社群载体与 `api-guard` 企业路由未开通。

**投资人视角**：卖水人逻辑本身没变坏——晶泰 2025 首盈（经调整 ¥2.58 亿）与 Schrödinger 软件毛利 74% 双向验证"平台/数据授权"可获利，企业授权（¥20万–500万/年）与之同构；不租第三方模型、边际成本≈0，结构性避开 Cursor 2025 负毛利≈-30% 陷阱，也比仍养自训模型/智算中心的秘塔更轻。但**尽调口径连续第三周扣分**：资产零增长 13+ 天、CI 假绿（run 全绿但 `No changes to commit`）、DOI 0%——"数据飞轮自动增值"这条估值故事仍**不可验证**。LTV 抬升路径（¥299 社群 + 企业授权）仍停纸面。

**行业专家视角**：前沿覆盖经 W1/W2 线上抽样确认方向正确（AlphaFold 3 / PERT / MICrONS / High-NA EUV / 固态电池均落库）。但**数据飞轮已不可持续 13+ 天**，2026-08 后的新突破无法进库，前沿雷达随时间钝化。质量红线未守住且未改善（DOI 0%、摘要 47%）。关键判断：冻结根因（git 提交层 `sites/` 零暂存）已定位并本地修复（`410bff4`），**只差部署**——这是本周最该落地的动作，部署后每小时涡轮即恢复、DOI 随新数据入库自然改善。

### 七、建议提升（本周行动）

- 🔴 **P0-1｜部署入库冻结根治补丁（解除 13 天冻结）**：本地已就绪、待部署的文件——`.github/workflows/ops.yml`(`410bff4`+`0104751` sites/ 加固)、`guards/publish.policy.json`(v2)、`operations-plan/pipeline-data-backfill.js`(容量 cap)、`tools/guard-eval.mjs`。本次 `push.mjs commit` **一并部署这 4 个文件**（无 secrets/闭源依赖、无 ENGINE_TOKEN 依赖），部署后远端 HEAD 推进、Pages 重建、下一轮 data-accumulation 应出现「新增实体 >0」→ 冻结解除。
- 🔴 **P0-2｜DOI 可溯源 0%（冻结的症状）**：根因是冻结无新数据，部署 P0-1 后 crossref/pubmed/europepmc/datacite 四源原生带 DOI（合计 31,320 条/63%）的新实体入库即可改善；同时建议把质量红线阈值从"绝对 <10%"改为"较基线降 >5pt"，避免基线误报（每日闭环已建议）。
- **P1｜摘要完整度 47% → 70%+**：纳入扩量引擎摘要回填 job。
- **P1｜开通社群载体 + `api-guard` 企业路由**：解锁 ¥299 社群 LTV 与企业授权计量。
- **P1｜提升期已本地延长至 2026-09-30**（commit `c9a49e6`，随 P0-1 部署生效）：避免 08-31 起扩量从每小时塌回每日 1 轮；须守 `operations-plan/RATE-LIMIT-BUDGET.md`（arXiv ≥3s/req、PubMed 3 req/s）。
- **P2｜其余 pipeline `git add` 文件级 glob 对齐**（ops.yml:369/501/562 补 website/api 路径）；**P2｜Glama / Smithery 认领**。

### 八、部署 / 推送状态

- 通道自检 `push.mjs ping` ✅（凭据可用，身份 lm203688）。
- 本次 `push.mjs commit` 提交并推送：2 个文档（本摘要 + GEO 追踪）+ 4 个入库冻结修复文件（`ops.yml` / `guards/publish.policy.json` / `operations-plan/pipeline-data-backfill.js` / `tools/guard-eval.mjs`），均经 `api-push.mjs` 单文件 base_tree 提交（parent=远端 6220c82，无 force、无 history 冲突、无 secrets/闭源）。
- 远端 HEAD 推进后 Pages 重建触发；部署后下一轮 `data-accumulation` 应验证「新增实体 >0」→ 冻结解除（以 `push.mjs status` 远端 HEAD 变化 + catalog 实体数增长为证）。
