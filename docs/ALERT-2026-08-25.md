# ⚠️ ALERT — GeneTech 14站 数据库扩张健康巡检

**日期**：2026-08-25（本地）
**状态**：🟡 STALL ×8 连续 + ⚠️ QUALITY
**告警等级**：高（须用户手动干预）

## 核心指标
| 指标 | 本日 | 上次日（08-24） | Δ |
|---|---|---|---|
| 总实体 | 49879 | 49879 | **+0（连续第 9 天冻结，08-17 起）** |
| 站点数 | 22 | 22 | +0 |
| 状态 | 🟡 STALL ⚠️ QUALITY | 🟡 STALL ⚠️ QUALITY | 持平 |

## 数据质量（红线）
- 溯源 URL 完整度：99%（正常）
- 摘要完整度：47%（较前次 47% 无下滑）
- **DOI 可溯源度：0%（< 10% 红线，触发 ⚠️ QUALITY）**
- 跨源去重率：97%（正常）
- 唯一键：48249 / 49879

## 根因（已定位，仍未修复）
1. 🔴 高 — ops.yml decide 步骤 cron 字符串漂移（仍比对 `*/30 * * * *`，实际已改为 `0 * * * *`），数据涡轮分支永不命中，`should_run_data` 仅靠 `HOUR==12` 兜底 → 每日仅约 2 轮，吞吐降约 12×。
2. 🟡 中 — 即便这 2 轮跑完也新增 0 条且游标未推进；疑「站点×检索词」分页空间已走完，需扩检索词或加源。

## 远端只读复核（本日实测）
- 经 `raw.githubusercontent.com` 拉取远端 `state/backfill-cursor.json`，**与本地游标逐字节一致**（14 键同值）→ 排除本地副本过期，**确证 CI 侧冻结**。
- 游标最后变更 2026-08-14T12:48Z → **冻结已满 11 天**。
- ops.yml 仍每小时运行、逐轮 `completed / success` 但零数据提交 → 「绿色空转」持续，静默失败机制未变。

## 告警触达状态（关键）
- ⚠️ **Agent Mail 告警邮件通道连续第 4 天失效（08-22~08-25）**：GetMe 返回「授权已失效，请前往 workbuddy://agentmail 重新授权，当前请勿重试」。
- 本告警改以本文件（`docs/ALERT-2026-08-25.md`）+ 对话回复触达，作为邮件通道不可用时的补偿。**用户须重新授权 Agent Mail 方能恢复邮件告警。**

## 建议动作（本自动化按约束未改代码、未推远端）
1. 修 decide 漂移：把 `"*/30 * * * *"` 比对改为 `"0 * * * *"`（或改为「非 `30 12 * * *` 的 schedule 均视为数据轮」）。
2. 核查扩量为何新增 0：手动 `workflow_dispatch` 跑一次 data，看日志里 `新增实体合计` 与游标 diff；若确为检索词耗尽，则补检索词 / 加数据源。
3. 推送闭源 refactor 前，先到 Settings → Secrets → Actions 配好 `ENGINE_TOKEN`（否则推送即全红）。
4. 补真告警：`notify-on-failure` 覆盖不到「跳过 / 软失败 / 零新增」，建议加零新增守门（连续 N 轮 `ADDED=0` 自动建 Issue）。
5. DOI 可溯源度长期 0%：扩量入库未落 DOI 字段，建议 backfill 落盘时保留 Crossref / DataCite 的 DOI。
6. **立即重新授权 Agent Mail（workbuddy://agentmail）**，恢复巡检告警邮件触达。

> 由「每日数据库扩张健康巡检」自动化维护。约束：不注册账号、不碰私钥、不替用户签约、绝不自行推送远端。
