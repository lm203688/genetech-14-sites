# GeneTech 14 站 · 推广自动化策略（GEO / SEO）

> 原则：**能机器自跑的获客动作全部闭环化（cron 流水线），人工只做一次「配密钥」。**
> 一切推广产出均自动部署、自动提交、自动上报，无需人工持续介入。

---

## 一、自动化推广架构（自运行闭环）

```
数据增量(每30分) ─┐
                  ├─► build-site.mjs ─► GitHub Pages 自动部署
GEO内容生成(每日) ─┘        │  (llms.txt / rss.xml / 博客 / JSON-LD)
                           ▼
            pipeline-geo-promotion.js（每日 12:30 UTC，ops-extra 触发）
                   ├─ 1. 自动写 GEO 博客 → content/blog/*.md → 触发部署
                   ├─ 2. IndexNow 推 Bing/Yandex（配 KEY 后自动）
                   ├─ 3. Google/Bing sitemap 主动 ping（无需密钥，永远跑）
                   ├─ 4. 自动设置 GitHub Topics（GITHUB_TOKEN，零额外密钥）
                   ├─ 5. 可选：自动发 dev.to（配 KEY 后自动）
                   └─ 6. 自动更新「🚀 GEO 自动推广状态」Issue（人工零查看）
```

## 二、各渠道的自动化等级

| 渠道 | 自动化 | 机制 | 人工一次性动作 |
|---|---|---|---|
| **AI/LLM 可发现性（GEO）** | ✅ 全自动 | `llms.txt` + `rss.xml` + JSON-LD + `/api/catalog.json` + MCP，每次部署刷新 | 无 |
| **GEO 内容弹药** | ✅ 全自动 | 流水线按新增实体自动生成速览博客 → 自动部署 | 无 |
| **Bing/Yandex 收录** | ✅ 配 KEY 后自动 | IndexNow 每日推送全站 URL | 仓库 Secrets 设 `INDEXNOW_KEY` |
| **Google/Bing 主动 ping** | ✅ 全自动 | 每次运行 GET sitemap ping（无需密钥） | 无 |
| **GitHub Topics** | ✅ 全自动 | `gh api` 设置（用内置 GITHUB_TOKEN） | 无 |
| **MCP 目录（Glama）** | ✅ 全自动 | 公开仓库 + `glama.json` 自动索引 | 用 GitHub 登录连接一次 |
| **MCP 目录（Smithery）** | ✅ 配后自动 | `smithery.yaml` 已就绪，连仪表盘发布 | 仪表盘连接一次 |
| **dev.to 自动发文** | ✅ 配 KEY 后自动 | 流水线调用 dev.to API 发速览文 | 仓库 Secrets 设 `DEV_TO_API_KEY` |
| **状态可见性** | ✅ 全自动 | 自动维护「🚀 GEO 自动推广状态」Issue | 无 |

## 三、唯一需要你手动做的一次性操作（之后系统自跑）

1. **仓库 Settings → Secrets 增加 2 个（可选但强烈建议）：**
   - `INDEXNOW_KEY`：到 [Bing Webmaster Tools](https://www.bing.com/webmasters) 添加站点后生成。设好后 Bing/Yandex 每日主动收录。
   - `DEV_TO_API_KEY`：到 [dev.to](https://dev.to/settings/extensions) 生成。设好后每次新文章自动同步发布到 dev.to（开发者 GEO 高价值渠道）。
2. **MCP 目录连接（各一次，点几下即可，之后自动索引）：**
   - Glama：用 GitHub 登录 → Add Server → 选本仓库（已含 `glama.json`）。
   - Smithery：仪表盘导入本仓库（已含 `smithery.yaml`）。
3. **Google Search Console（一次性）：** 添加属性 `lm203688.github.io/genetech-14-sites` 并提交 `…/sitemap.xml`。之后 sitemap 每日刷新，新增 URL 自动被抓取。

> 不做 1–3 也不影响系统运行：Google/Bing ping、GitHub Topics、GEO 内容、llms.txt/rss、状态 Issue **全部零密钥自跑**。配了密钥只是多开两个高价值渠道。

## 四、为什么这叫「GEO」而不是传统 SEO

GEO（Generative Engine Optimization）= 让 **AI Agent / 大模型** 在生成答案时引用你。本项目天然契合：
- 数据 **结构化**（schema.org Dataset / SoftwareApplication / Article JSON-LD）；
- 事实 **可溯源**（每条实体带原始论文 URL + 置信度）；
- 接口 **机器可读**（`entities.json` + MCP + `llms.txt`）；
- 内容 **持续自动产出**（流水线每日生成速览，给 LLM 现成可引用材料）。

这正是 Elicit / Consensus 之后「Agent 原生知识底座」的红利窗口。

## 五、如何验证在跑

- 看 `🚀 GEO 自动推广状态` Issue（每次运行自动更新，含各渠道状态码）；
- 看 `reports/report-geo-promotion-*.json`（CI Artifact 保留 30 天）；
- 线上校验：`/llms.txt`、`/rss.xml`、`/blog/` 是否随数据更新而变化。

## 六、下一步可叠加（仍保持自动化）

- 用真实 LLM（配 `OPENAI_API_KEY` 等）把速览文润色成深度综述（仍是定时自动跑）；
- 自动提交到更多目录（Product Hunt、HackerNews 需人工，但可自动生成草稿 Issue）；
- 流量看板：接入 Cloudflare/Plausible 后由 `finance-track` 一并出周报。
