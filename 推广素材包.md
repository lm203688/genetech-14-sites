# GeneTech 14站知识引擎 — 推广素材包（SEO / GEO 获客用）

> 用途：本项目已上线静态站（GitHub Pages）+ MCP 包（npm）。本文件是**可直接复制发布**的推广文案，
> 覆盖搜索引擎收录、开发者目录、社区发帖三大获客渠道。改完即发，无需再写。
> 在线站：`https://lm203688.github.io/genetech-14-sites/`
> MCP 调用：`npx -y @genetech/data-mcp`

---

## 0. 一句话定位（Pitch，随处可用）
**GeneTech 14站知识引擎** —— 14 个生命科学领域的结构化知识库（基因/蛋白/药物/临床/组学…共 2809 条实体），
提供**真·混合检索**（BM25 + 字段加权 + RRF）与**机器可读 API**；开发者一条命令 `npx -y @genetech/data-mcp`
就能让 AI Agent 直接检索，无需自己爬库。

---

## 1. 英文发帖（Hacker News / dev.to / Reddit r/bioinformatics、r/MachineLearning）

**标题（HN）：Show HN: A 14-domain biomedical knowledge engine with hybrid search + an MCP server**
**标题（dev.to）：** Build an AI Agent that queries 14 biomedical knowledge bases via one MCP server

**正文：**
```
I got tired of scraping PubMed/OpenAlex/arXiv every time an agent needed a
biomedical fact. So I bundled 14 domain knowledge bases (genetics, proteins,
drugs, clinical, omics… 2809 curated entities) into one engine with:

- Real hybrid search (BM25 + field weighting + RRF fusion), not fake "semantic" wrappers
- Machine-readable APIs per site (index.json / entities.json) — easy to pipe into RAG
- Structured data (JSON-LD) on every page for GEO / LLM citation
- An MCP server so any agent can query it:  npx -y @genetech/data-mcp

Live site: https://lm203688.github.io/genetech-14-sites/
3 deep-dive posts (why MCP for agents / how the hybrid search works / the GEO advantage):
https://lm203688.github.io/genetech-14-sites/blog/

Open source, zero-cost to self-host. Feedback / domain requests welcome.
```

---

## 2. 中文发帖（知乎 / 少数派 / V2EX / 掘金）

**标题：** 我做了一个 14 站生物医学知识引擎，AI Agent 一条命令就能查（附混合检索原理）
**正文：**
```
做科研/做 Agent 的人应该都有过这种痛：每次要一个生物医学事实，就得去爬 PubMed、OpenAlex、arXiv。
我把 14 个领域的知识库（基因、蛋白、药物、临床、组学……共 2809 条结构化实体）合成了一个引擎：

- 真·混合检索：BM25 + 字段加权 + RRF 融合，不是套个"语义"壳的伪检索
- 每站都有机器可读 API（index.json / entities.json），直接喂 RAG
- 每个页面带 JSON-LD 结构化数据，方便大模型引用（GEO）
- 给了个 MCP Server，Agent 一行就能连：  npx -y @genetech/data-mcp

在线体验：https://lm203688.github.io/genetech-14-sites/
三篇深挖：为什么给 Agent 做 MCP / 混合检索怎么实现 / 生成式引擎优化(GEO)的优势
都在 /blog/ 下。

开源、可自托管、零成本。欢迎提领域需求和建议。
```

---

## 3. 开发者目录（Glama / Smithery）—— 决定 Agent 生态能否发现你

**Glama 描述（提交时用）：**
```
GeneTech Data MCP — query 14 biomedical knowledge bases (genetics, proteins,
drugs, clinical, omics; 2809 curated entities) with hybrid search (BM25 + field
weighting + RRF). Machine-readable per-site APIs for RAG piping. One command:
npx -y @genetech/data-mcp
```
- 提交入口：https://glama.ai/mcp/servers  → Add server → 贴 GitHub 仓库 URL
- 仓库已含 `glama.json`（安装命令已写好），登录 GitHub 即认领。

**Smithery 描述（同类）：** 同上要点，强调 "plug into any MCP client, no scraping needed"。

---

## 4. 社交短帖（Twitter / X，中英双语各一条）

**EN:** Shipping a 14-domain biomedical knowledge engine with real hybrid search + an MCP server.
Agents can query it with one command: `npx -y @genetech/data-mcp` — no more scraping PubMed.
https://lm203688.github.io/genetech-14-sites/

**中：** 做了个 14 站生物医学知识引擎，真混合检索 + 机器可读 API；Agent 一行命令
`npx -y @genetech/data-mcp` 就能查，不用自己爬库。https://lm203688.github.io/genetech-14-sites/

---

## 5. 搜索引擎收录（必须做，否则站白建）

1. **Google Search Console**：https://search.google.com/search-console → 添加属性
   `https://lm203688.github.io/genetech-14-sites/` → 验证（用 GitHub Pages 的 DNS / HTML 标签）→
   提交 `https://lm203688.github.io/genetech-14-sites/sitemap.xml`。
2. **Bing Webmaster + IndexNow**（已接好，只差密钥）：
   - 在 Bing Webmaster Tools 生成一个 IndexNow key；
   - 仓库 **Settings → Secrets → Actions** 新增 `INDEXNOW_KEY` = 该 key（构建会自动写入
     `.well-known/indexnow.txt` 并由 promotion 管线提交 Bing/Yandex）；
   - 同时新增 `INDEXNOW_KEY_LOCATION`（一般留空即可，构建已写死路径
     `/.well-known/indexnow.txt`）。
3. **Yandex Webmaster**：同 Bing，IndexNow 通用。
4. **GitHub Topics**：仓库页面 → About → gear → 加 `genetics` `bioinformatics` `knowledge-base`
   `mcp` `rag` `biomedical` `semantic-search` `open-data`。

---

## 6. 发布后追踪（看流量从哪来）
- Search Console / Bing 后台看收录与展现。
- Glama / Smithery 看 MCP 安装/调用量。
- npm：`npm stats @genetech/data-mcp` 看下载。
- 站自带结构化数据，可用 https://search.google.com/test/rich-results 验证 JSON-LD。
